# Dialled In — Connected extension

Not built yet — see the root `CLAUDE.md` for the plan.

Same in-page experience as the standalone edition, but generation goes through the
Dialled In portal: no API key on the user's machine, settings (pitch, personas, tones,
model) managed centrally, and every draft logged for review.
